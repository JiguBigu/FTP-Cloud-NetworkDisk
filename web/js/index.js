$(document).ready(init);

function init(){
  console.log("init current page")
  listFiles();      //初始化时列出文件
  /*$.ajax({
    type:"post",
    url:"",
    data:{"username":username, "password":password}
    
  })*/
}